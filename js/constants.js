// Shared constants for the topology optimization application

// Visualization threshold - elements with density below this won't be rendered
export const DENSITY_THRESHOLD = 0.3;

// Numerical stability constants
export const EPSILON = 1e-12;
export const CG_TOLERANCE = 1e-8;
